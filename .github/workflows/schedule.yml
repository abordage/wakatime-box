name: Update Gist
on:
  schedule:
    - cron: "50 4 * * *"

  workflow_dispatch:

jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Update gist
        uses: abordage/wakatime-box@master
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          WAKA_API_KEY: ${{ secrets.WAKA_API_KEY }}

          MAX_RESULT: 5
          DATE_RANGE: last_7_days # supported: last_7_days, last_30_days, last_6_months, last_year
          GIST_ID: 4ef3fc74af8d5febf26b55b8209ed24b # REPLACE WITH YOUR GIST ID
